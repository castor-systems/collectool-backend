{
  "name": "collectool-backend",
  "version": "0.1.0",
  "bin": {
    "collectool-backend": "bin/collectool-backend.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.1119.0",
    "jest": "^30"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.248.0",
    "constructs": "^10.5.0"
  }
}
